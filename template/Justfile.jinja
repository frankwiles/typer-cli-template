# {{ project_name }} - Development tasks
# Use `just --list` to see available commands

# Default recipe - show help
default:
    just --list

# Install/sync dependencies
sync:
    uv sync

# Run the CLI with arguments
run *ARGS:
    uv run {{ cli_name }} {{ ARGS }}

# Run tests
test:
    uv run pytest

# Format code with ruff
fmt:
    uv run ruff format .

# Lint code with ruff
lint:
    uv run ruff check .

# Type check with ty
typecheck:
    uv run ty check

# Run all checks (lint, format check, typecheck, test)
check: lint typecheck test
    uv run ruff format . --check

# Build the package
build:
    uv build

# Clean build artifacts
clean:
    rm -rf dist/ .pytest_cache/ .ruff_cache/
    find . -type d -name "__pycache__" -exec rm -rf {} +

# Install pre-commit hooks
pre-commit:
    uv run pre-commit install

# Run pre-commit on all files
pre-commit-all:
    uv run pre-commit run --all-files

# Run the CLI via uvx from a git repo (for testing distribution)
uvx REPO *ARGS:
    uvx --from git+{{ REPO }} {{ cli_name }} {{ ARGS }}

# Start a TUI development server (for textual apps)
tui-dev:
    uv run textual run --dev src/{{ package_name }}/tui.py
